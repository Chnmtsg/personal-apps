{
  "name": "where-am-i",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A goal measurement PWA. Refuses to store a goal until it has been turned into numbers.",
  "scripts": {
    "dev": "vite",
    "build": "node tools/icons.js && vite build",
    "preview": "vite preview",
    "test": "node --test test/**/*.test.js",
    "icons": "node tools/icons.js",
    "verify": "npm test && npm run build",
    "preview:screens": "node tools/preview.js"
  },
  "devDependencies": {
    "fake-indexeddb": "^6.0.0",
    "fast-check": "^3.22.0",
    "vite": "^5.4.0"
  }
}
