{
  "name": "english-feedback",
  "private": true,
  "type": "module",
  "workspaces": [
    "app",
    "worker"
  ],
  "scripts": {
    "dev:app": "npm run dev -w app",
    "dev:worker": "npm run dev -w worker",
    "test": "node --test \"tests/*.test.ts\"",
    "check": "tsc --noEmit -p app && tsc --noEmit -p worker && tsc --noEmit -p tests",
    "verify": "npm run check && npm test",
    "build": "npm run verify && npm run build -w app",
    "deploy:worker": "npm run verify && npm run deploy -w worker"
  },
  "allowScripts": {
    "esbuild@0.28.1": true,
    "workerd@1.20260801.1": true
  },
  "devDependencies": {
    "@types/node": "^26.2.0"
  }
}
